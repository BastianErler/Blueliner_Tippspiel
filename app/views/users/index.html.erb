<% provide(:title, 'All users') %>
<h1>Alle Tipper</h1>
<div class="table-responsive">
<table class="table table-hover">
  <thead>
    <tr>
      <td>Platz</td>
      <td>Name</td>
      <td>Kosten</td>
      <% if current_user.admin? %>
        <td> Einzahlungen </td>
        <td> Saldo </td>
        <td> Einzahlen </td>
        <td> Löschen </td>
      <%end%>
    </tr>
  </thead>
  <tbody>
    <% @place = 0 %>
    <% @price = 0 %>
    <%= render @users %>
  </tbody>
</table>
</div>
